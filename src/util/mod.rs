


/// Utility objects for parsing the HTTP `Range` header.
pub mod byterange;

/// Utility objects for extracting path components.
pub mod path;

/// Implementation of a simple file server, serving static files from a directory.
pub mod fs;
